# Copy this file to .env and adjust for your machine:
#   cp .env.example .env

# API base URL (no trailing slash)
VITE_API_URL=http://localhost:3001

# true = in-memory mock data; false = call the real server
VITE_USE_MOCK=true
